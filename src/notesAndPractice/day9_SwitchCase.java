package notesAndPractice;

public class day9_SwitchCase {

/*
 Switch Case is use when there are number of different task to do and have number of options.
 Switch:
 Case:
 Break: Good practice is to have a break after each case so that program will end if solution meet.
        If there is no break point then it will flow to subsequent case till end.
 Default:

switch (expression) {
    >> Case 1:
            >> Block of work;
                >> Break;

     >> Case 2:
            >> Block of work;
                >> Break;

     >> Case 3:
            >> Block of work;
                >> Break;

     >> Default:
            >> Break;
 }
 Statement ;



//menu-driven programming

 */
}
